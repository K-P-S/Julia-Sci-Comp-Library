#runtime: in O(n^2.81)
#accepts square matrices A,B (assumes nxn, mxm where n,m are powers of two)
#returns matrix C

function partition(A)
  #add try-catch
  #accepts square matrix A
  n = length(A)[1]
  for 

function multiply(A,B)
  n = length(A)[1]
  #new matrix C
  C = new zeroesMatrix(length(A)[1],length(B)[2])
  if n == 1
    C[1:1] = A[1:1] * B[1:1]
  else
    
  
 

